version: "3.9"

services:
  backend:
    build: .
    container_name: agentgraph-backend
    ports:
      - "9000:9000"
    env_file:
      - .env
    volumes:
      - .:/app
    command: ["python", "main.py", "--host", "0.0.0.0", "--port", "9000"]
