// DailyUsage model removed: the project now uses DailyLogin for per-day
// markers and UsageInterval as a fallback. This file is kept as a noop
// placeholder to avoid requiring/deletion churn in historical branches.

module.exports = {};
